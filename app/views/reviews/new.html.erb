<h2>Review This Restaurant</h2>

<%= form_for [@restaurant, @review] do |f| %>
    
    <%= f.label :title %>
    <%= f.text_area :title %>
    <%= f.label :content %>
    <%= f.text_area :content %>
   <%= f.submit "Submit" %>

<% end %>