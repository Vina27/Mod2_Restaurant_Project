<h2>Review This Restaurant</h2>

<% if @errors%>
 <%= @errors.map do |error|%>
    <%= error %>
    <%end%> 
<%end%> 

<%= form_for @review do |f| %>
    
    <%= f.label :title %>
    <%= f.text_area :title %>
    <%= f.label :content %>
    <%= f.text_area :content %>
   <%= f.submit "Submit" %>

<% end %>

<%# collection_select and then remove @restaurants%>
<%# find a way to pass in users info by adding collection select by having users name %>